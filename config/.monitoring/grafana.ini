[server]
http_port = 3000
domain = localhost

[security]
admin_user = admin
admin_password = nexus_monitoring_2025

[datasources]
prometheus = {'type': 'prometheus', 'url': 'http://prometheus:9090', 'access': 'proxy', 'isDefault': True}
