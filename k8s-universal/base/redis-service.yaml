apiVersion: v1
kind: Service
metadata:
  name: nexus-redis-service
  namespace: nexus-platform
  labels:
    app: nexus-redis
    component: cache
spec:
  selector:
    app: nexus-redis
  ports:
  - name: redis
    port: 6379
    targetPort: 6379
    protocol: TCP
  type: ClusterIP
