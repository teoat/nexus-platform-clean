apiVersion: v1
kind: Service
metadata:
  name: nexus-postgres-service
  namespace: nexus-platform
  labels:
    app: nexus-postgres
    component: database
spec:
  selector:
    app: nexus-postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
    protocol: TCP
  type: ClusterIP
