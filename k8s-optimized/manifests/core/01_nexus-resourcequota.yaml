apiVersion: v1
kind: ResourceQuota
metadata:
  name: nexus-quota
  namespace: nexus
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    persistentvolumeclaims: "10"
    services: "10"
    secrets: "20"
    configmaps: "20"
